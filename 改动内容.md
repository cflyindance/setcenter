
已按「结束时间不得早于开始时间」加上限制和校验，修改如下。

## 1. 结束时间最小值（applyEndMin）

- **结束日期**：`endDateInput.min = 开始日期`，不能选早于开始日期的日期。
- **结束时间**：当结束日期与开始日期相同时，`endTimeInput.min = 开始时间`；当结束日期晚于开始日期时，去掉结束时间的 `min`。
- 开始日期、开始时间、结束日期变化时都会重新执行 `applyEndMin()`，保证结束的 min 始终正确。

## 2. 开始时间变化时联动

- 开始日期、开始时间的 `change` / `input` 会先执行 `applyStartMin()`，再执行 `applyEndMin()`，保证结束的 min 随开始时间更新。
- 结束日期的 `change` / `input` 也会执行 `applyEndMin()`，用于在结束日期改变时更新结束时间的 `min`。

## 3. 结束时间「Now」按钮

- 点击结束时间的「Now」时：若当前日期时间早于开始日期时间，则把结束日期、结束时间设为与开始相同；否则设为当前日期时间。
- 设置后会调用 `applyEndMin()`。

## 4. 页面加载

- 在已有 `applyStartMin()` 之后增加一次 `applyEndMin()`，确保一进入页面结束时间的 min 就正确。

## 5. 提交校验

- 点击「完成」时，若选择了「自定义」且填了开始、结束：在原有「开始时间不得早于下发时间」校验之后，增加「结束时间不得早于开始时间」校验。
- 若 `结束日期 < 开始日期` 或 `结束日期 = 开始日期 且 结束时间 < 开始时间`，则提示「结束时间不得早于开始时间，请重新选择。」并阻止跳转。

这样结束时间在选值和提交时都不会早于开始时间。






已按「生效开始时间不早于下发时间」做完逻辑和校验，修改总结如下。

## 1. distribute-time.html（保存下发时间）

- **「Next →」改为带 id 的链接**，并在点击时拦截默认跳转。
- **点击「Next →」时**：
  - 若选的是「自定义」且已选日期、时间：把当前选的日期、时间写入 `localStorage`（`distribute_date`、`distribute_time`）。
  - 若选「自定义」但未选完整：用当前日期时间写入再跳转。
  - 若选「立即」：用当前日期时间写入再跳转。
- 写入后再执行 `window.location.href = 'effective-time.html'` 跳转。

## 2. effective-time.html（限制与校验）

- **读取下发时间**：页面加载时从 `localStorage` 读取 `distribute_date`、`distribute_time`。
- **开始时间最小值**：
  - 开始日期：`startDateInput.min = distribute_date`，不能选早于下发日期的日期。
  - 当开始日期等于下发日期时：`startTimeInput.min = distribute_time`，不能选早于下发时间的时刻。
  - 当开始日期晚于下发日期时：去掉开始时间的 `min`，任意时间均可。
- **开始日期变化时**：监听开始日期的 `change`/`input`，根据是否等于下发日期更新开始时间的 `min`。
- **默认开始时间**：若存在下发时间，则默认开始时间设为下发时间；否则设为当前时间。点击「Now」时，若当前时间早于下发时间，则用下发时间填充开始时间。
- **完成时校验**：点击「完成」且选的是「自定义」时，若生效开始日期+时间早于下发日期+时间，则提示「生效开始时间不得早于下发时间，请重新选择。」并阻止跳转。

这样生效开始时间在界面和提交时都不会早于 distribute-time 页面选择的下发时间。

---

## 下发时间与生效时间展示及状态标签

### 1. kiosk-theme-list.html（屏保列表页）

#### 下发门店入口带 themeId
- 每个屏保卡片的「下发门店」改为带主题 ID 的链接：`store-select.html?themeId=xxx`

#### 卡片展示下发时间与生效时间
- **下发时间**：有则显示（如 `2025/12/04 14:30`），否则显示 `--`
- **生效时间**：有则显示（自定义为「开始 - 结束」，立即为「立即生效」），否则显示 `--`
- **创建时间**：保持原样

#### 状态标签
根据下发时间和生效时间自动计算并展示状态标签：

| 下发状态 | 条件 | 样式 |
|---------|------|------|
| **待下发** | 有下发时间，但当前时间 < 下发时间 | 黄色标签 |
| **已下发** | 当前时间 >= 下发时间 | 绿色标签 |
| 无标签 | 未设置下发时间 | -- |

| 生效状态 | 条件 | 样式 |
|---------|------|------|
| **待生效** | 已设置生效时间，但当前时间 < 生效开始时间（或"立即生效"但还未下发） | 黄色标签 |
| **已生效** | 当前时间在生效时间范围内（或"立即生效"且已下发） | 绿色标签 |
| **已失效** | 当前时间 > 生效结束时间 | 灰色标签 |
| 无标签 | 未设置生效时间 | -- |

状态标签显示在主题名称右侧，与名称同行排列。

### 2. store-select.html（选择门店页）
- 页面加载时从 URL 解析 `themeId`，并写入 `localStorage` 的 `current_theme_id_for_distribute`，供后续流程使用。

### 3. effective-time.html（生效时间页 - 完成时写回主题）
- 用户点击「完成」并确认后，在跳转回列表前：
  - 从 `localStorage` 读取 `current_theme_id_for_distribute` 得到当前主题 ID
  - 从 `localStorage` 读取 `distribute_date`、`distribute_time` 拼成**下发时间**字符串写入该主题的 `distributeTime`
  - 根据「自定义」或「立即」：
    - **立即**：该主题的 `effectTime` 设为「立即生效」
    - **自定义**：用表单的开始/结束日期时间拼成「开始时间 - 结束时间」写入该主题的 `effectTime`
  - 把更新后的主题列表写回 `localStorage`，并清除 `current_theme_id_for_distribute`

---

## 屏保图片数据同步（创建与编辑）

### 1. screensaver-create.html（新建屏保）
- 提交时把**竖版、横版图片**一并写入主题：
  - 用 `FileReader.readAsDataURL` 把每个文件转成 data URL
  - 主题对象增加 `portraitImages`、`landscapeImages` 数组，每项为 `{ dataUrl: "data:image/..." }`
  - 再和名称、创建时间等一起存入 `localStorage` 的 `kiosk_themes`

### 2. screensaver-edit.html（编辑屏保）

#### 从主题加载数据
- 根据 URL 的 `id` 取当前主题
- 主题名称填入「主题名称」输入框
- 若存在 `theme.portraitImages` / `theme.landscapeImages`，则按这些数据渲染竖版、横版预览（与创建页一致的预览 + 删除 + 上传槽，最多 5 张）

#### 编辑与保存
- 支持在原有图片基础上：删除某张、继续上传新图（仍为最多 5 张）
- 点击「确认」时：保留已有 dataUrl 的图片，新选的图片用 `FileReader` 转成 dataUrl，写回该主题的 `portraitImages`、`landscapeImages` 和 `name`，并保持 `id`、`createTime`、`effectTime`、`distributeTime` 等不变

---

## 屏保列表图片展示与预览

### 1. 列表页展示屏保图片（kiosk-theme-list.html）

#### 按分类展示
- 在每条屏保卡片中，**时间信息后面**新增屏保图片展示区域
- 从主题的 `portraitImages` 和 `landscapeImages` 数组中读取已上传的图片
- **竖版**：如果有竖版图片，显示「竖版:」标签 + 图片列表（竖向小图 `48×64px`）
- **横版**：如果有横版图片，显示「横版:」标签 + 图片列表（横向小图 `64×48px`）
- **无图片不显示**：如果某分类没有上传图片，则该分类整行不展示

### 2. 图片预览功能（kiosk-theme-list.html）

#### 新增图片预览模态框
- 全屏黑色半透明背景（`bg-black/80`）
- 居中显示大图（最大 90vw × 85vh，保持比例）
- 右上角关闭按钮（×）
- 左右切换按钮（< >），只有多张图片时显示
- 底部显示当前图片序号（如 `1 / 3`）

#### 小图可点击
- 列表中的屏保小图添加 `cursor-pointer` 样式和 hover 效果
- 点击小图打开预览模态框，直接显示被点击的那张

#### 多图切换
- 点击左右按钮或使用键盘方向键（← →）切换图片
- 循环切换：最后一张按"下一张"跳到第一张，反之亦然
- 按 `Esc` 键或点击背景关闭预览

#### 交互细节
- 竖版/横版图片分别管理，点击某个分类的图片只在该分类内切换
- 单张图片时隐藏切换按钮
- 切换时实时更新底部计数器

---

## 新建屏保错误处理优化

### screensaver-create.html 改动

#### 问题修复
- 修复了保存失败时静默忽略错误的问题
- 原代码在 `localStorage.setItem` 失败时会静默跳转到列表页，导致数据未保存但用户不知情

#### 改进内容
- **错误提示**：当 localStorage 保存失败（如存储空间不足）时，显示友好的错误提示
- **阻止跳转**：保存失败时不再跳转到列表页，让用户有机会重试或处理问题
- **控制台日志**：添加 `console.error` 输出详细错误信息，便于调试

---

## 图片压缩优化 - 支持创建多个屏保

### 问题原因
- localStorage 存储空间有限（约 5-10MB）
- 原代码直接将图片转成 base64 存储，一张大图可能就占用几 MB
- 创建多个屏保后很快就会超出存储限制

### 解决方案 - 图片压缩

#### screensaver-create.html 改动
- 新增 `compressImage(file, maxWidth, maxHeight, quality)` 函数
  - 使用 Canvas 压缩图片
  - 竖版图片最大尺寸：540×960px
  - 横版图片最大尺寸：960×540px
  - 压缩质量：70%（JPEG 格式）
- 新增 `processImage(item, isPortrait)` 函数
  - 根据图片类型选择合适的压缩参数
- 修改提交逻辑，使用压缩后的图片数据

#### screensaver-edit.html 改动
- 同样添加图片压缩功能
- 已有的图片（dataUrl）保持不变，只压缩新上传的图片
- 添加保存失败的错误处理

#### 效果
- 图片存储体积减少约 80-90%
- 可以创建更多的屏保主题
- 预览和大图查看仍然清晰可用

---

## 下发门店状态展示

### 1. store-select.html 改动
- 点击"Next →"时，将选中的门店列表保存到 `localStorage`（`selected_stores_for_distribute`）
- 门店信息包含：门店名称（name）、MID

### 2. effective-time.html 改动
- 完成下发时，从 `localStorage` 读取选中的门店
- 为每个门店生成下发状态（模拟）：success（成功）、pending（下发中）、failed（失败）
- 将门店及状态信息存入主题的 `distributedStores` 字段
- 清理临时存储的门店数据

### 3. kiosk-theme-list.html 改动

#### 卡片中展示下发门店状态
- 显示下发门店统计：X 成功、X 下发中、X 失败
- 提供"查看详情"按钮

#### 新增门店详情模态框
- 顶部统计栏：总计、成功、下发中、失败数量
- 门店列表：每条显示门店名称、MID、状态标签
- 状态标签样式：
  - 成功：绿色标签
  - 下发中：黄色标签
  - 失败：红色标签
- 支持 Esc 键或点击背景关闭

#### 效果示例
```
下发门店: 3 成功 1 下发中 1 失败 [查看详情]
```

---

## 下发状态标签优化

### kiosk-theme-list.html 改动

#### 新增下发状态类型
根据门店下发情况，显示更精确的下发状态：

| 状态 | 条件 | 样式 |
|------|------|------|
| **待下发** | 下发时间未到 | 黄色标签 |
| **下发中** | 有门店正在下发中 | 蓝色标签 |
| **已下发** | 全部门店下发成功 | 绿色标签 |
| **部分下发失败** | 有成功也有失败的门店 | 橙色标签 |
| **下发失败** | 全部门店下发失败 | 红色标签 |

#### 逻辑说明
- `computeDistributeStatus` 函数增加 `distributedStores` 参数
- 根据门店的成功/失败/下发中数量判断整体下发状态
- 优先级：部分下发失败 > 下发失败 > 下发中 > 已下发

---

## 下发状态模拟优化

### effective-time.html 改动

#### 随机场景模拟
每次下发门店时，随机选择一个场景来模拟不同的下发结果：

| 场景 | 概率 | 说明 |
|------|------|------|
| **全部成功** | 20% | 所有门店状态为 success |
| **部分失败** | 30% | 奇数位置成功，偶数位置失败 |
| **全部失败** | 20% | 所有门店状态为 failed |
| **下发中** | 30% | 混合状态（40% 成功、30% 下发中、30% 失败） |

#### 效果
- 更容易模拟出各种下发状态
- 可以看到"部分下发失败"、"下发失败"、"下发中"等不同状态标签

---

## 失败门店重新下发功能

### kiosk-theme-list.html 改动

#### 门店详情模态框新增"重新下发失败门店"按钮
- 按钮位于统计栏右侧
- 只有存在失败门店时才显示
- 点击后弹出确认提示

#### 重新下发逻辑
- 只对状态为 `failed` 的门店进行重新下发
- 重新下发后随机分配新状态：
  - 70% 概率变为成功
  - 20% 概率变为下发中
  - 10% 概率仍然失败
- 更新 localStorage 并刷新模态框和列表页显示

#### 效果
- 用户可以在详情中一键重新下发所有失败的门店
- 重新下发后立即看到最新状态
- 列表页的状态标签也会同步更新

---

## 导航栏固定布局优化

### common.css 改动

#### 布局调整
- `.app-layout`: 设置 `height: 100vh` 和 `overflow: hidden`，页面不再整体滚动
- `.sidebar`: 设置 `height: 100vh`，侧边栏固定在可视区域
- `.sidebar nav`: 设置 `flex: 1` 和 `overflow-y: auto`，导航菜单区域独立滚动
- `.main-content`: 设置 `height: 100vh` 和 `overflow-y: auto`，主内容区域独立滚动

#### 效果
- 侧边栏始终固定在左侧可视区域
- 右侧内容区域独立滚动，不影响侧边栏
- 当导航菜单过多时，导航区域可独立滚动

---

## 屏保活动智能排序

### kiosk-theme-list.html 改动

#### 排序逻辑
根据业务优先级对屏保活动进行排序，需要关注的问题排在前面：

| 优先级 | 状态 | 说明 |
|--------|------|------|
| 1 | 下发失败 / 部分下发失败 | 需要立即处理的问题 |
| 2 | 下发中 | 正在进行，需要关注 |
| 3 | 待下发 | 即将执行的任务 |
| 4 | 待生效 | 已下发但还未生效 |
| 5 | 已生效 | 正常运行中 |
| 6 | 已失效 | 历史记录 |
| 7 | 未设置 | 新创建未配置 |

#### 同优先级排序
- 同一优先级内，按创建时间倒序排列
- 最新创建的活动排在前面

#### 设计理由
- **问题优先**：下发失败的活动需要立即关注和处理
- **进行中次之**：正在下发的活动需要持续关注
- **待处理排中间**：待下发和待生效的活动需要按计划执行
- **正常运行靠后**：已生效的活动正常运行，无需特别关注
- **历史最后**：已失效的活动仅供查阅

---

### 23. screensaver-create.html 新增"下发门店"按钮

#### 需求
在新建屏保页面的确定按钮旁边新增"下发门店"按钮，点击后保存屏保数据并进入下发门店流程。

#### 修改内容

**screensaver-create.html**
- 在页头右侧添加"下发门店"按钮（蓝色边框样式）
- 提取保存逻辑为公共函数 `saveThemeAndRedirect(redirectUrl)`
- "确认"按钮：保存后跳转到 `kiosk-theme-list.html`
- "下发门店"按钮：保存后跳转到 `store-select.html?themeId={themeId}`

#### 按钮样式
```html
<div class="flex items-center gap-2">
  <button type="button" id="distributeBtn" class="px-4 py-2 rounded-ant border border-primary text-primary hover:bg-blue-50">下发门店</button>
  <button type="submit" form="screensaverForm" class="px-4 py-2 rounded-ant bg-primary text-white hover:bg-blue-600">确认</button>
</div>
```

#### 流程说明
点击"下发门店"按钮后：
1. 验证主题名称
2. 处理并压缩上传的图片
3. 保存屏保主题到 localStorage
4. 跳转到 store-select.html?themeId=xxx
5. 继续选择门店 → 选择下发时间 → 选择生效时间 完整流程

---

### 24. 屏保素材必填验证

#### 需求
新建/编辑屏保时，屏保素材为必填项，至少需要上传一个竖版或横版的图片/视频素材。

#### 修改文件
- `screensaver-create.html`
- `screensaver-edit.html`

#### 验证逻辑
```javascript
if (portraitList.length === 0 && landscapeList.length === 0) {
  alert('请至少上传一个竖版或横版的屏保素材');
  return;
}
```

#### 验证顺序
1. 验证主题名称（必填）
2. 验证屏保素材（至少一个竖版或横版）
3. 处理图片并保存

---

### 25. 下发流程页面左上角图标改为返回箭头

#### 需求
将选择门店、选择下发时间、选择生效时间页面的左上角关闭图标（✕）换成返回箭头（←），避免误导用户。

#### 修改文件
- `store-select.html`
- `distribute-time.html`
- `effective-time.html`

#### 修改内容
```html
<!-- 修改前 -->
<a href="..." class="p-2 rounded hover:bg-gray-100 text-gray-600" aria-label="关闭">✕</a>

<!-- 修改后 -->
<a href="..." class="p-2 rounded hover:bg-gray-100 text-gray-600" aria-label="返回">←</a>
```

#### 页面返回链接
- `store-select.html` → 返回到 `kiosk-theme-list.html`
- `distribute-time.html` → 返回到 `store-select.html`
- `effective-time.html` → 返回到 `distribute-time.html`

---

### 26. 视频上传类型限制

#### 需求
选择上传视频时，只能上传视频文件，不可上传图片；选择上传图片时，只能上传图片文件。

#### 修改文件
- `screensaver-create.html`
- `screensaver-edit.html`

#### 实现逻辑

**1. 动态切换 accept 属性**
```javascript
function getMediaType(role) {
  var radioName = role === 'portrait' ? 'portraitType' : 'landscapeType';
  var checked = document.querySelector('input[name="' + radioName + '"]:checked');
  return checked ? checked.value : 'image';
}

function updateAccept(role) {
  var inputId = role === 'portrait' ? 'filePortrait' : 'fileLandscape';
  var input = document.getElementById(inputId);
  var type = getMediaType(role);
  input.accept = type === 'video' ? 'video/*' : 'image/*';
}
```

**2. 文件类型验证**
```javascript
function addFiles(role, files) {
  var mediaType = getMediaType(role);
  for (var i = 0; i < files.length && list.length < MAX_IMAGES; i++) {
    var file = files[i];
    if (mediaType === 'video') {
      if (!file.type.startsWith('video/')) continue;
    } else {
      if (!file.type.startsWith('image/')) continue;
    }
    list.push({ url: URL.createObjectURL(file), file: file, type: ... });
  }
}
```

**3. 切换类型时清空已上传文件**
- 切换图片/视频类型时，清空当前已上传的文件列表
- 更新 file input 的 accept 属性
- 重新渲染预览区域

**4. 视频预览**
- 视频文件显示带播放图标的缩略图
- 使用 `<video>` 标签显示视频封面

**5. 视频保存**
- 视频使用 FileReader 转为 data URL 保存
- 保存时包含 type 字段区分图片和视频

---

### 27. 图片/视频上传数量限制与动态提示

#### 需求
根据选择的媒体类型（图片/视频）和方向（竖版/横版）显示不同的提示信息，并限制上传数量。

#### 规则

| 类型 | 方向 | 建议尺寸 | 格式 | 大小限制 | 数量限制 |
|------|------|----------|------|----------|----------|
| 图片 | 竖版 | 1080*1920px | JPEG、PNG、JPG、GIF | 1M以内 | 最多5张 |
| 图片 | 横版 | 1920*1080px | JPEG、PNG、JPG、GIF | 1M以内 | 最多5张 |
| 视频 | 竖版 | 1080*1920px | MP4、AVI等主流 | 10M以内 | 最多1个 |
| 视频 | 横版 | 1920*1080px | MP4、AVI等主流 | 10M以内 | 最多1个 |

#### 修改文件
- `screensaver-create.html`
- `screensaver-edit.html`

#### 实现逻辑

**1. 动态数量限制**
```javascript
function getMaxCount(role) {
  return getMediaType(role) === 'video' ? 1 : 5;
}
```

**2. 动态提示更新**
```javascript
function updateHints(role) {
  var mediaType = getMediaType(role);
  var size = isPortrait ? '1080*1920px' : '1920*1080px';
  if (mediaType === 'video') {
    // 显示视频提示
  } else {
    // 显示图片提示
  }
}
```

**3. 视频单选限制**
```javascript
input.multiple = type !== 'video';  // 视频不允许多选
```

**4. 切换类型时更新**
- 切换图片/视频类型时自动更新提示文字
- 同时更新 file input 的 multiple 属性

---

### 28. 全局 Alert 改为 Toast 提示

#### 需求
将系统中所有的 `alert()` 弹窗提示改为更友好的 Toast 提示方式。

#### 新增文件
- `assets/toast.js` - Toast 组件 JS
- `assets/common.css` - 新增 Toast 样式

#### 修改文件
- `screensaver-create.html`
- `screensaver-edit.html`
- `material.html`
- `effective-time.html`

#### Toast 组件 API
```javascript
Toast.success('操作成功');     // 成功提示（绿色图标）
Toast.error('操作失败');       // 错误提示（红色图标）
Toast.warning('请填写必填项'); // 警告提示（橙色图标）
Toast.info('提示信息');        // 信息提示（蓝色图标）
```

#### Toast 样式特点
- 固定在页面顶部居中显示
- 带有对应类型的图标（成功✓、错误×、警告⚠、信息ℹ）
- 自动消失（默认3秒）
- 入场/出场动画效果
- 支持多个 Toast 同时显示

#### 替换规则
| 原代码 | 替换为 |
|--------|--------|
| `alert('请填写...')` | `Toast.warning('请填写...')` |
| `alert('保存失败...')` | `Toast.error('保存失败...')` |
| `alert('...不得早于...')` | `Toast.warning('...不得早于...')` |

---

### 29. kiosk-theme-list 分页功能

#### 需求
为屏保主题列表添加分页展示功能。

#### 修改文件
- `kiosk-theme-list.html`

#### 分页功能特点

**1. 分页状态管理**
```javascript
var currentPage = 1;  // 当前页码
var pageSize = 20;    // 每页条数
```

**2. 分页计算**
- 根据数据总数和每页条数计算总页数
- 对排序后的数据进行切片，只渲染当前页的数据
- 自动修正越界页码

**3. 页码显示**
- 最多显示5个页码按钮
- 首尾页始终可见（当数据足够多时）
- 中间省略号表示跳过的页码
- 当前页码高亮显示

**4. 分页控件**
- 上一页/下一页按钮（禁用状态正确显示）
- 可点击的页码按钮
- 每页条数选择器（10/20/50条）

**5. 分页 UI 布局**
```html
<div class="flex items-center gap-2">
  <button id="prevPageBtn">&lt;</button>
  <div id="pageNumbers"><!-- 页码按钮 --></div>
  <button id="nextPageBtn">&gt;</button>
  <select id="pageSizeSelect">
    <option value="10">10 条/页</option>
    <option value="20" selected>20 条/页</option>
    <option value="50">50 条/页</option>
  </select>
</div>
```

**6. 交互行为**
- 切换每页条数时自动回到第一页
- 点击页码或上下页按钮立即刷新列表
- 总条数实时显示

---

### 30. 上传素材来源选择功能

#### 需求
在新建屏保页面点击上传时，支持选择从本地上传或从图片素材库选择。本地上传时可选择是否同步保存到素材库。

#### 修改文件
- `screensaver-create.html`

#### 功能说明

**1. 上传来源选择模态框**
点击上传槽后弹出选择框：
- **本地上传**：从电脑选择文件上传
- **素材库选择**：从图片素材库中选择

**2. 本地上传选项**
- 选择图片类型时，显示"同时保存到图片素材库"复选框
- 勾选后，上传的图片会自动压缩并保存到素材库的第一个分类
- 视频不支持保存到素材库

**3. 素材库选择模态框**
- 左侧显示素材分类列表
- 右侧显示当前分类下的图片网格
- 支持多选（受最大数量限制）
- 选中的图片显示蓝色边框和勾选图标
- 底部显示已选数量和确认/取消按钮

**4. 数据共享**
- 与 `material.html` 页面共享素材数据（localStorage）
- 分类数据存储在 `material_categories`
- 图片数据存储在 `material_images_{categoryId}`

**5. 交互流程**
```
点击上传槽
    ↓
弹出"选择上传方式"模态框
    ↓
┌─────────────────┬─────────────────┐
│    本地上传     │   素材库选择    │
├─────────────────┼─────────────────┤
│ 打开文件选择器  │ 打开素材库模态框│
│ (可选保存到库)  │ 选择已有素材    │
└─────────────────┴─────────────────┘
    ↓
添加到预览列表
```

**6. 限制**
- 视频类型只支持本地上传，不支持素材库选择
- 选择素材数量受最大数量限制（图片5张，视频1个）

**7. 布局优化**
- "同时保存到图片素材库"复选框整合到"本地上传"卡片内
- 复选框显示在本地上传按钮下方，作为本地上传的附属选项
- 视觉上更清晰地表明该选项仅针对本地上传操作
```
┌─────────────────────────┐
│ 📁 本地上传             │
│    从电脑选择文件上传   │
├─────────────────────────┤
│ ☑ 同时保存到图片素材库  │  ← 仅图片类型显示
└─────────────────────────┘

┌─────────────────────────┐
│ 🖼️ 素材库选择           │
│    从图片素材库中选择   │
└─────────────────────────┘
```

**8. 素材库数据共享**
- 与 `material.html` 页面共享同一套数据结构
- `material_categories`：分类名称列表（字符串数组）
- `material_images`：所有图片列表，每个图片有 `category` 字段
- 素材卡片样式与 material.html 保持一致：
  - 4:3 比例卡片
  - 右上角显示分类标签（带颜色，无分类时不显示）
  - 左上角显示选中勾选图标
  - 底部显示素材名称
- 分类侧边栏样式与 material.html 一致（选中高亮）
- 空状态提示用户前往图片素材页面上传

**9. 本地上传素材的分类处理**
- 本地上传同步到素材库的图片，`category` 设为空字符串
- 不属于任何具体分类，只能在"全部"中查看
- 不显示分类标签
- 修改文件：
  - `screensaver-create.html`：保存时不设置 category
  - `material.html`：渲染时判断 category 是否为空，为空则不显示标签

---

### 31. kiosk-theme-list 筛选条件增强

#### 需求
在屏保主题列表页面增加下发状态、生效状态筛选条件。

#### 修改文件
- `kiosk-theme-list.html`

#### 筛选条件

| 筛选项 | 类型 | 选项 |
|--------|------|------|
| 主题名称 | 文本输入 | 模糊匹配 |
| 下发状态 | 下拉选择 | 待下发、下发中、已下发、部分下发失败、下发失败 |
| 生效状态 | 下拉选择 | 待生效、已生效、已失效 |
| 创建时间 | 日期范围 | 开始日期 → 结束日期 |

#### 筛选逻辑
```javascript
list = list.filter(function(t) {
  // 主题名称模糊匹配
  if (filterName && t.name.indexOf(filterName) < 0) return false;
  // 下发状态精确匹配
  var distStatus = computeDistributeStatus(...);
  if (filterDistStatus && distStatus.text !== filterDistStatus) return false;
  // 生效状态精确匹配
  var effStatus = computeEffectStatus(...);
  if (filterEffStatus && effStatus.text !== filterEffStatus) return false;
  // 创建时间范围筛选
  if (filterStartDate && createDate < startD) return false;
  if (filterEndDate && createDate > endD) return false;
  return true;
});
```

#### 交互特性
- 所有筛选条件实时生效
- 筛选后自动回到第一页
- 重置按钮清空所有筛选条件
- 将状态计算函数移到 render 函数外部以便复用

---

### 32. material.html 图片移动分类功能

#### 需求
在图片素材页面中，支持将图片移动到其他分类。

#### 修改文件
- `material.html`

#### 新增功能
1. **下拉菜单新增"移动到"选项**
   - 在每张图片的操作下拉菜单中添加"移动到"按钮
   - 位于"编辑"和"裁剪"之间

2. **移动分类模态框**
   - 显示当前图片名称
   - 下拉选择目标分类（排除"全部"）
   - 当前分类默认选中
   - 支持点击遮罩或取消按钮关闭

3. **移动逻辑**
   - 更新图片的 `category` 字段
   - 根据新分类自动更新 `tagColor`
   - 移动成功后显示 Toast 提示
   - 自动刷新图片列表